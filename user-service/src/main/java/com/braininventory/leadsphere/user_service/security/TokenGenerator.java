package com.braininventory.leadsphere.user_service.security;import org.springframework.stereotype.Component;import java.security.SecureRandom;import java.util.Base64;@Componentpublic class TokenGenerator {    private static final String ALPHA_NUMERIC = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$";    private final SecureRandom secureRandom = new SecureRandom();    /**     * Generates a human-readable but secure temporary password     */    public String generateTemporaryPassword() {        int length = 12; // Sufficient for temporary use        StringBuilder password = new StringBuilder(length);        for (int i = 0; i < length; i++) {            int index = secureRandom.nextInt(ALPHA_NUMERIC.length());            password.append(ALPHA_NUMERIC.charAt(index));        }        return password.toString();    }}