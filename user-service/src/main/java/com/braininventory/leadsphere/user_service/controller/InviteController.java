package com.braininventory.leadsphere.user_service.controller;import com.braininventory.leadsphere.user_service.dto.*;import com.braininventory.leadsphere.user_service.exception.DuplicateInviteException;import com.braininventory.leadsphere.user_service.exception.InvalidRequestException;import com.braininventory.leadsphere.user_service.service.InviteService;import jakarta.servlet.http.HttpServletRequest;import jakarta.validation.Valid;import jakarta.validation.constraints.Email;import jakarta.validation.constraints.NotBlank;import lombok.Data;import org.springframework.http.HttpStatus;import org.springframework.http.ResponseEntity;import org.springframework.security.access.prepost.PreAuthorize;import org.springframework.web.bind.annotation.*;@RestController@RequestMapping("/sales-user")public class InviteController {    private final InviteService inviteService;    // Constructor injection (recommended)    public InviteController(InviteService inviteService) {        this.inviteService = inviteService;    }    // Create a simple DTO for the request    public record InviteRequest(            @NotBlank @Email String email,            @NotBlank String firstName,            @NotBlank String lastName    ) {}    @PostMapping("/invite")    public ResponseEntity<StandardResponse<String>> createInvite(            @RequestBody @Valid InviteRequest request) {        inviteService.createInvite(request);        return ResponseEntity.ok(                StandardResponse.ok(request.email(), "Invitation sent successfully")        );    }}